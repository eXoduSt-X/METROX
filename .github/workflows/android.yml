name: Android CI

on:
  push:
    branches: [ main, Add-video ] # Añadida tu rama para que se dispare con tus commits actuales
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Make gradlew executable
        run: chmod +x ./gradlew
      - name: Lint Android
        run: ./gradlew lint

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: check
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'
      - uses: gradle/actions/setup-gradle@v4
      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # =========================================================================
      # SCRIPT DE AUDITORÍA JNI: Extrae la radiografía real de símbolos del .so
      # =========================================================================
      - name: Extraer y listar símbolos JNI reales del binario
        run: |
          echo "=== LISTANDO SÍMBOLOS JNI DE LA CLASE CONFIG ==="
          mkdir -p temp_jnidump
          unzip -q app/libs/ffmpeg-kit-16kb-full.aar -d temp_jnidump
          find temp_jnidump -name "libffmpegkit.so" -path "*arm64-v8a*" -exec nm -D {} \; | grep "FFmpegKitConfig" || true
          rm -rf temp_jnidump

      - name: Build
        run: ./gradlew app:assemble

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: METROX-Debug-APK
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
