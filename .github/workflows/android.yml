name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v4
      - name: Make gradlew executable
        run: chmod +x ./gradlew
      - name: Lint Android
        run: ./gradlew lint

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: check
    steps:
      - uses: actions/checkout@v4
      - name: Download FFmpegKit
        run: |
          mkdir -p app/libs
          curl -L https://github.com/arthenica/ffmpeg-kit/releases/download/v6.0.2/ffmpeg-kit-min-gpl-6.0.2.aar -o app/libs/ffmpeg-kit-min-gpl-6.0.2.aar
          # Renombramos para que coincida con el nombre que Gradle espera
          mv app/libs/ffmpeg-kit-min-gpl-6.0.2.aar app/libs/ffmpeg-kit-min.aar
          
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'
      - uses: gradle/actions/setup-gradle@v4
      - name: Make gradlew executable
        run: chmod +x ./gradlew
      - name: Build
        run: ./gradlew app:assemble

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: METROX-Debug-APK
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
